@using Microsoft.AspNetCore.Http
@using System.Collections.Generic;
@model List<ImageStore>
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor

@{
    ViewData["Title"] = "GameResults";

    var playerName = @HttpContextAccessor.HttpContext.Session.GetString("playerName");
    var playerScore = @HttpContextAccessor.HttpContext.Session.GetInt32("playerScore");
    
}


    <body>
        <header class="masthead bg-primary text-white text-center">
            <div class="container d-flex align-items-center flex-column">

                <!-- Masthead Heading -->
                <h3 class="masthead-subheading font-weight-light mb-0">Results</h3>
                <div class="masthead-subheading font-weight-light mb-0">Player: @playerName</div>
                <div class="masthead-subheading font-weight-light mb-0">Score: @playerScore out of 10</div>


                @if (playerScore == 10)
                {
                    <div class="masthead-subheading font-weight-light mb-0">Perfect 10, didn't laugh once!</div>
                }

                @if (@Model != null && playerScore < 10)
                {

                    <div class="container">
                        <div class="masthead-subheading font-weight-light mb-0">Caught ya laughing!</div>
                        <div class="row">
                            @foreach (var image in Model)
                            {
                                <div class="col-sm">
                                    <img src="@image.ImageBase64String" width="160px" />
                                </div>
                            }
                        </div>
                    </div>
                }


                <!-- Icon Divider -->
                <div class="divider-custom divider-light">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="divider-custom-line"></div>
                </div>

                <!-- Masthead Subheading -->
                <p class="masthead-subheading font-weight-light mb-0"></p>

            </div>
        </header>
    </body>




